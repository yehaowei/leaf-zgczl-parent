<div style="${size};overflow:auto;${style}" class="${wrapClass}" id="${id}">${searchField}<div class="functionTree" style="height:${bodyheight}px;overflow:auto"><div atype="tree.body"></div></div></div>
<script>
	new Leaf.Tree(${config});
	$le('${id}').bind('${bindtarget}');
</script>