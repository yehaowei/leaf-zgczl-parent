<textarea tabindex="${tabindex}" class="${wrapClass} item-tf" autocomplete="off" atype="input" style="width:${width}px;height:${height}px;${fontstyle};${style}" id="${id}"></textarea>
<script>
	new Leaf.TextArea(${config});
	${binding}
</script>